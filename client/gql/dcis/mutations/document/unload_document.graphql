mutation UnloadDocument($documentId: ID!, $additional: [String!] = []) {
  unloadDocument(input: { documentId: $documentId, additional: $additional }) {
    success
    errors {
      field
      messages
      __typename
    }
    src
    __typename
  }
}
