mutation UnloadFileValueArchive($documentId: ID!, $sheetId: Int!, $columnId: Int!, $rowId: Int!, $name: String!) {
  unloadFileValueArchive(input: {
    documentId: $documentId,
    sheetId: $sheetId,
    columnId: $columnId,
    rowId: $rowId,
    name: $name,
  }) {
    success
    errors {
      field
      messages
      __typename
    }
    src
    __typename
  }
}
