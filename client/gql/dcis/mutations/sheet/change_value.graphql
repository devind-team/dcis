mutation ChangeValue($documentId: ID!, $sheetId: ID!, $columnId: ID!, $rowId: ID!, $value: String!) {
  changeValue(input: {
    documentId: $documentId,
    sheetId: $sheetId,
    columnId: $columnId,
    rowId: $rowId,
    value: $value
  }) {
    success
    value
    updatedAt
  }
}
