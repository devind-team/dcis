mutation UnloadAggregationsInFile($periodId: ID!) {
  unloadAggregationsInFile(input: { periodId: $periodId }) {
    success
    errors {
      field
      messages
      __typename
    }
    src
    __typename
  }
}
