#import "../fragments/sheet_fields.graphql"

query ReportSheet($sheetId: ID!, $reportDocuments: [ReportDocumentInputType!]!, $mainDocumentId: ID) {
  reportSheet(sheetId: $sheetId, reportDocuments: $reportDocuments, mainDocumentId: $mainDocumentId) {
    ...SheetFields
  }
}
