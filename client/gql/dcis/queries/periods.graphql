#import "../fragments/period_fields.graphql"
#import "../../core/fragments/user_fields.graphql"

query Periods($userId: ID!) {
  periods(userId: $userId) {
    ...PeriodFields
    periodGroups {
      id
      name
      createdAt
      users {
        ...UserFields
      }
      privileges {
        id
        name
        key
        __typename
      }
      __typename
    }
    __typename
  }
}
