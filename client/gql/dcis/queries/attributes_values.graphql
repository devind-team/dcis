#import "../fragments/attribute_fields.graphql"

query AttributesValues($documentId: ID!) {
  attributesValues(documentId: $documentId) {
    ...AttributeFields
    value {
      id
      value
      createdAt
      updatedAt
      __typename
    }
  }
}
