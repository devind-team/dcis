#import "../../core/fragments/file_fields.graphql"

query ValueFiles(
  $documentId: ID!,
  $sheetId: Int!,
  $columnId: Int!,
  $rowId: Int!
) {
  valueFiles(
    documentId: $documentId,
    sheetId: $sheetId,
    columnId: $columnId,
    rowId: $rowId
  ) {
    ...FileFields
  }
}
