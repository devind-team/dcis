#import "../fragments/privilege_fields.graphql"

query UserIndividualPrivileges($userId: ID, $periodId: ID!) {
  userIndividualPrivileges(userId: $userId, periodId: $periodId) {
    ...PrivilegesFields
  }
}
